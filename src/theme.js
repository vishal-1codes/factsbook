import {createMuiTheme} from '@material-ui/icons/styles';

const theme = createMuiTheme({
    palette:{
        primary:{
            main:"#764abc"
        }
    },
    background:{
        default: '#fff'
    }
  
})

export default theme;